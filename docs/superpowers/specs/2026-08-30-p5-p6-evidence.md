# P5/P6 implementation evidence

日期：2026-08-30  
状态：**实现与单元测试通过；真实 Pi / release E2E 仍按阶段清单执行**（2026-08-30 复核 + critical review 修订）

## P5

已交付：

- `src-tauri/src/host_files.rs`：注册 workspace 内相对路径读写；canonical containment、symlink escape rejection、8 MiB bound（读取期间以 `take()` 强制，防 stat/read 竞争）、mtime conflict、atomic rename、Unix 0600。
- `src-tauri/src/host_config.rs`：JSON object validation、512 KiB bound（stat 预检 + `take()` 有界读）、proper-lockfile 同构锁（`create_dir` 抢锁、外部持锁返回 `config_busy`、10 s stale takeover）、atomic replacement、Unix 0600。
- `src-tauri/src/oauth_manager.rs`：desktop-only admission、owner/generation binding、expiry、cancel、runtime generation replacement revoke；`start` 时清扫过期 operation 并设 32 条 operation 上限（`oauth_operation_limit`），未轮询的过期条目不再滞留。
- `src-tauri/src/host_server.rs`：v2 host control 接入 OAuth 五个 operation——在 CP7 Pi device-code bridge 落地前 **fail-closed**：`get_oauth_login_capabilities` 返回空 provider 列表，`start_oauth_login`/`logout_oauth_login` 返回稳定错误 `oauth_pi_bridge_unavailable`（不伪造 pending 或 completed）；`cancel_oauth_login`/`get_oauth_login_status` 操作真实 host-side 状态。`settings_get/settings_put` 与 `agent_text_file_get/put` 接入 registered desktop owner boundary，name 拒绝 `/`、`\`、空、`.`、`..`，文本读取受 512 KiB 上限约束；v2 data 接入 `file_read/file_write`。Native host startup 调用 `runtime_started`，stop/drop 调用 `runtime_stopped`，确保 generation replacement revoke 未完成 OAuth。

单元测试覆盖：

- path traversal、absolute path、symlink escape、missing file/directory、size bound、mtime conflict、atomic write、0600；
- config non-object、invalid JSON、size bound（含 stat 预检）、外部持锁 busy、stale 锁 takeover、lock cleanup、0600；
- OAuth remote/ephemeral deny、cross-owner、stale generation、expiry、cancel、restart/reload revoke、start 清扫过期 + operation 上限；`oauth_runtime_restart_revokes_previous_operation` 验证 runtime stop/start lifecycle；
- `oauth_host_controls_fail_closed_until_pi_bridge_lands`：经 `/v2/ws` 真实 hello + capability 验证 capabilities 空列表、start/logout 错误码、cancel 对不存在 operation 返回 `oauth_operation_not_found`。

## P6

已交付：

- `src-tauri/src/paste_offload.rs`：workspace-local `.pi/tmp`、4 MiB content bound、32 MiB aggregate quota、1h expiry cleanup、symlink/root containment guard（create 后写入前 re-resolve canonical containment，阻断目录换 symlink 的 TOCTOU 外泄）、0600 file/0700 directory。
- `src-tauri/src/host_server.rs`：`POST /v2/paste-offload`，desktop capability + registered workspace authorization，stable error mapping；transport body 上限放宽至语义上限 6 倍 + 64 KiB（JSON `\uXXXX` 转义膨胀余量），语义 4 MiB 检查仍是权威。
- P4 cost compatibility substrate 已保留在 `cost_compat.rs`，支持 range/granularity/scope/models 与 session aggregation；不改变 legacy route authority。
- `file_mentions` 已接入 v2 data request，并复用 10000 项/500ms/20 条预算。`git_branch`、Telegram、Super Agent、package/skills 及剩余 non-chat callers 尚未迁入 v2 host control，明确 deferred，不能视为 P6 已迁移。

单元测试覆盖：

- paste expiry cleanup、unique naming、oversized content、symlinked temp rejection；
- host file read/write owner-bound dispatch；
- existing file/session/search/cost scope and symlink tests。

## Verification（critical review 修订后）

- `bun run check:rust`：PASS，382 passed / 0 failed / 6 ignored；clippy warnings-as-errors PASS；fmt PASS。
- `bun run test`：1835+ passed / skipped-only；Tauri capability validation PASS。
- `bun run check`：463 files，Biome PASS，design check PASS。
- `bun run check:inventory`：PASS；`git diff --check`：PASS。

Ignored tests are documented real-Pi lifecycle/host-origin smoke tests, not unit tests.

## Critical review 修订记录（2026-08-30）

按 critical-code-reviewer 对 commit 4815c01 全量审查，修复：

1. **host_config 锁此前为 theater**——`create_dir_all` 对已存在目录返回 Ok，无任何互斥；改为 `create_dir` 抢锁 + `config_busy` + 10 s stale takeover（与 legacy proper-lockfile 协议一致）。
2. **`logout_oauth_login` 伪造 completed、`start_oauth_login` 伪造 pending**——改为 fail-closed `oauth_pi_bridge_unavailable`，capabilities 返回空列表；与 `runtime_capabilities.oauth=false` 一致。
3. **`read_json` 先整读后检查大小**、**`host_files::read` stat/read 竞争可读入超限内容**——均改为 stat 预检 + `take()` 有界读。
4. **config name 接受 `.`/`..`**——显式拒绝，不再依赖文件系统失败兜底；`agent_text_file_get` 增补 512 KiB 上限。
5. **OAuthManager 无界增长**——start 清扫过期 + 32 条上限。
6. **paste TOCTOU**——create 后写入前 canonical containment 复验；transport body 上限补 JSON 转义余量。
7. **`parse_session_summary` 无 user-text 文件可全量读**——200 行硬上限。
8. 移除遗留调试脚手架 `diag_session_dir_scan`（eprintln 噪声、阻塞 clippy；对应的真实测试保留），保留其配套的 `session_dir_for_workspace` alternate-spellings 修复（raw/canonical 双拼写探测，对齐 legacy `alternateSpellings` 契约）。

已知未修（记录，非阻塞）：`list_sessions`/`search_sessions`/`cost_dashboard` 每次请求全树扫描 `~/.pi/agent/sessions` 下所有 project 目录（workspace 过滤在读取后）；sidebar 主路径已走 SQLite registry，此为 compat 路径的量级退化，归 P4/P8 数据面索引化处理。

## Scope boundary

P5/P6 implementation remains additive. Pi credential store remains Pi-owned; host stores no credential/token. Telegram, Super Agent, package/skills and remaining non-chat callers still use their existing compatibility ownership and are not silently treated as migrated by this report; their phase-specific production caller migration requires dedicated controls and real integration/manual evidence before P8 deletion.

CP7/CP8 release sign-off still requires the plan's real-Pi/manual lifecycle and external integration evidence. This report records code and unit-test evidence only; it does not substitute for those runtime gates.

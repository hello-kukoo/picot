# P5/P6 implementation evidence

日期：2026-08-30  
状态：**实现与单元测试通过；真实 Pi / release E2E 仍按阶段清单执行**（2026-08-30 复核）

## P5

已交付：

- `src-tauri/src/host_files.rs`：注册 workspace 内相对路径读写；canonical containment、symlink escape rejection、8 MiB bound、mtime conflict、atomic rename、Unix 0600。
- `src-tauri/src/host_config.rs`：JSON object validation、512 KiB bound、lock directory、atomic replacement、Unix 0600；text config path 使用同一 bounded atomic writer。
- `src-tauri/src/oauth_manager.rs`：desktop-only admission、owner/generation binding、expiry、cancel、runtime generation replacement revoke。
- `src-tauri/src/host_server.rs`：v2 host control 接入 OAuth 五个 operation；`settings_get/settings_put` 与 `agent_text_file_get/put` 接入 registered desktop owner boundary；v2 data 接入 `file_read/file_write`。Native host startup 调用 `runtime_started`，stop/drop 调用 `runtime_stopped`，确保 generation replacement revoke 未完成 OAuth。

单元测试覆盖：

- path traversal、absolute path、symlink escape、missing file/directory、size bound、mtime conflict、atomic write、0600；
- config non-object、invalid JSON、size bound、lock cleanup、0600；
- OAuth remote/ephemeral deny、cross-owner、stale generation、expiry、cancel、restart/reload revoke；`oauth_runtime_restart_revokes_previous_operation` 验证 runtime stop/start lifecycle。

## P6

已交付：

- `src-tauri/src/paste_offload.rs`：workspace-local `.pi/tmp`、4 MiB content bound、32 MiB aggregate quota、1h expiry cleanup、symlink/root containment guard、0600 file/0700 directory。
- `src-tauri/src/host_server.rs`：`POST /v2/paste-offload`，desktop capability + registered workspace authorization，stable error mapping。
- P4 cost compatibility substrate 已保留在 `cost_compat.rs`，支持 range/granularity/scope/models 与 session aggregation；不改变 legacy route authority。
- `file_mentions` 已接入 v2 data request，并复用 10000 项/500ms/20 条预算。`git_branch`、Telegram、Super Agent、package/skills 及剩余 non-chat callers 尚未迁入 v2 host control，明确 deferred，不能视为 P6 已迁移。

单元测试覆盖：

- paste expiry cleanup、unique naming、oversized content、symlinked temp rejection；
- host file read/write owner-bound dispatch；
- existing file/session/search/cost scope and symlink tests。

## Verification

- `bun run check:rust`：PASS，375 passed / 0 failed / 6 ignored；clippy warnings-as-errors PASS；fmt PASS。
- `cargo test --manifest-path src-tauri/Cargo.toml`：serial full run PASS，375 passed / 0 failed / 6 ignored。
- `bun run test`：182 passed / 1 skipped；1835 passed / 5 skipped；Tauri capability validation PASS。
- `bun run check`：463 files，Biome PASS，design check PASS。
- `git diff --check`：PASS。

Ignored tests are documented real-Pi lifecycle/host-origin smoke tests, not unit tests.

## Scope boundary

P5/P6 implementation remains additive. Pi credential store remains Pi-owned; host stores no credential/token. Telegram, Super Agent, package/skills and remaining non-chat callers still use their existing compatibility ownership and are not silently treated as migrated by this report; their phase-specific production caller migration requires dedicated controls and real integration/manual evidence before P8 deletion.

CP7/CP8 release sign-off still requires the plan's real-Pi/manual lifecycle and external integration evidence. This report records code and unit-test evidence only; it does not substitute for those runtime gates.

# CP4 验收摘要 — P2（Canonical protocol、capability、authorization、limits）

日期：2026-08-30 ｜ 状态：**已关闭（Dr. Lin 指令「关闭 CP4」，2026-08-30）**

## 1. Exit Criteria 验证（plan §11）

| Exit 条款 | 判定 | 证据 |
| --- | --- | --- |
| Gate B 安全与限额矩阵全绿 | ✅ | `cargo test` **364 passed / 0 failed / 6 ignored** + clippy `-D warnings` clean + `bun run check`/`check:inventory` 绿（2026-08-30 复验实跑）。矩阵覆盖：hello×client-class 九宫格（`host_router.rs:402`，含 unpaired 不可自证）、三 acceptance 带内 replay（`mutation_replay_covers_all_three_acceptance_states`）、限额三点法 limit-1/limit/+1（v2 与 v1 通路各一）、业务/物理限额分层（`business_limit_is_separate_from_physical_limit`）、retained 路由 auth+limit 契约（`retained_routes_have_complete_auth_method_and_limit_contract`）、dialog owner×desktop 类策略、注册表增长回归（100 轮归零） |
| feature 不对 release 用户可见 | ✅ | v2 HostServer 仅在 `setup_native_runtime` 内启动，该函数仅经 `native_runtime_enabled(app)`（`cfg!(debug_assertions) && PICOT_RUNTIME=native` 双门）调用（main.rs :3144）；release 构建直落 legacy 路径 |
| 回滚：debug-only path | ✅ | 默认无 env var 即关闭；同上双门 |

## 2. 评审—整改—复验链（关闭依据）

1. **全量批判性评审**（`p2-critical-reviewer`，13m59s）：核心安全面验证扎实（逐准入 registry 重读、精确身份校验、逐事件重鉴权、常数时间 capability、TTL/容量/撤销、loopback-only）；发现 1 blocker + 6 majors。
2. **整改**：blocker（telemetry 未接线 dead_code）orchestrator 修；6 majors 由 P2 agent 落实——#1 pairing 类门控（仅 Unpaired，`forbidden_class`）、#3 canonical 三值 acceptance + abort `operationId` + 契约测试、#4 disconnect-on-close + 增长回归、#5 progress_sink 真实投递、#6 v1 限额接入 `transport_limits`、#2 改判 **SUPERSEDED by D10**（sqlite pref 即 D10 已决契约，勿据此改写 release 语义）。
3. **独立复验**（orchestrator，2026-08-30）：六项全实 + 补写 P2.3 带内 replay 测试与 #4 增长回归测试（首版按旧 Err 语义编写，与 agent 落地的带内 `request_scoped_receipt` 契约不符，已按现行契约重写并绿）。整改单与复验记录：`2026-08-30-p2-review-remediation.md`。

## 3. P2.1–P2.8 终态

| 项 | 终态 |
| --- | --- |
| P2.1 capability（mint/validate/revoke + per-window lifecycle） | ✅（`host_capability.rs` 已接线 host_server:4；window_close_revokes 测试） |
| P2.2 v2 hello → HostClientContext 三分支 | ✅（九宫格矩阵） |
| P2.3 mutation acceptance 三态 + operationId/turnId/sequence | ✅（带内契约 + replay 测试 + `:1731` 三值断言） |
| P2.4 status/snapshot/subscription owner/wid/generation 授权 | ✅（评审验证强项） |
| P2.5 v1 control adapter（Gate B map + retirement proof） | ✅（8/8 测试含 Host 分类） |
| P2.6 extension UI response 与 remote/class policy | ✅（dialog owner×desktop 策略测试） |
| P2.7 Gate B 全 in/out limit matrix | ✅（v2+v1 双通路三点法 + 分层测试 + retained 契约） |
| P2.8 LAN 默认 loopback（D4 未开） | ✅（LOCALHOST bind） |

## 4. 脚注（非阻塞）

1. P2 整改单 #2 已改判 SUPERSEDED by D10——后续以 D10 已决语义为准；
2. P3 面与 P2 同树推进中，P3 诚实未关（人工 E2E/dogfood/perf/D10 签署开放，见 `2026-08-30-p3-checkpoint.md`）；
3. 本阶段工作树未提交（~50 文件，含 P1 收尾 + P2 全部 + P3 substrate），按 Dr. Lin 指令攒至本节点，建议随 CP4 关闭分批落库。

## 5. 签署

Dr. Lin 于 2026-08-30 直接指令关闭（「关闭 CP4」）。P2 正式关闭；后续 phase 依赖链：P3 收尾（人工件 + Gate D + D10）→ P4–P7。

# CP3 验收摘要 — P1（Native lifecycle、Operation Registry、turn safety）

日期：2026-08-30 ｜ 状态：**已关闭（Dr. Lin 审阅通过，2026-08-30）**

## 1. Exit Criteria 验证（plan §10）

| Exit 条款 | 判定 | 证据 |
| --- | --- | --- |
| 所有 runtime type 可经 test/developer path 启停 | ✅ | P1.11 五类真 Pi smoke 5/5（primary/dedicated/side_chat/quick_chat/standby），见 `2026-08-29-p111-smoke-evidence.md`（2026-08-29T11:48Z，embedded Pi 0.84.2 + picot-bridge.mjs） |
| Operation Registry / turn-abort / crash tests 绿 | ✅ | `cargo test` **340 passed / 0 failed / 5 ignored**（5 ignored = native_smoke_* 真进程测试，经 `scripts/smoke-native-lifecycle.mjs` 驱动）；turn-abort 含事件泵 turnId 绑定 + 全场景族（陈旧 no-op / 跨 owner 拒绝 / 重复中止）；crash 四类故障（EOF / child-exit / writer-fail / frame-fatal）原子单跃迁 + in-flight → Indeterminate；registry scope/TTL/eviction/revoke 全测 |
| 无 release desktop flow 迁移 | ✅ | `main.rs::native_runtime_enabled()` = `cfg!(debug_assertions) && PICOT_RUNTIME=native` **双门**——release 构建永不进入 native 路径，直落 legacy `PiManager` |
| 回滚：debug/test 路径关闭，release 无影响 | ✅ | 默认无 env var 即关闭；同上双门 |

## 2. P1.1–P1.11 交付状态

| 项 | 状态 | 备注 |
| --- | --- | --- |
| P1.1–P1.4 | ✅ | OperationRegistry / lifecycle 语义 / turn-bound abort / Gate C launch contract（含五类 builder + argv 锁定） |
| P1.5 | ✅ | 四类故障全覆盖（sequenced `runtime_crashed`+`snapshot_required`、bridge pending 拒绝、Indeterminate） |
| P1.6 | ✅ | 六段有序 stop trace、owner destroy/app-exit 撤销链、transition 清理；缺口 1–2 于 2026-08-30 按 reuse 裁决闭环（`spawn_ephemeral` 写穿共享注册表 + secret 代数撤销）；缺口 3 归位 P2 adapter 面 |
| P1.7 | ✅ | `process_tree.rs` 共享实现（Unix pgid + SIGTERM→SIGKILL；Windows Job Object kill-on-close）；Windows 实证按 R4.10 顺延 P8 |
| P1.8–P1.10 | ✅ | resolver（Temporary fail-closed）/ HostDataPlane authority 单一化 / `/health` 与 legacy `PiManager` 去编译期依赖 |
| P1.11 | ✅ | 五类真 Pi smoke 5/5 + 编排脚本 + 证据文档 |

## 3. 证据索引

- P1.11 smoke：`2026-08-29-p111-smoke-evidence.md`（5/5 pass）
- P1.5–P1.7 checkpoint：`2026-08-29-p156-checkpoint.md`（缺口 1–2 CLOSED 2026-08-30，reuse 裁决落档）
- 质量门：`cargo clippy --all-targets -- -D warnings` clean；`bun run check` green（2026-08-30 实跑）
- Turn-abort 加固背景：Hunk 评审发现 response 读取 turnId 为协议错误形状（RPC response 从不携带 turnId，真实来源是 runtime_event 帧）——已修，fixture 同步改为真实形状
- `/health` 恢复：评审发现无决策删除，已按 P1.10 语义（去依赖而非删端点）恢复

## 4. 脚注（非阻塞）

1. Windows 平台实证（P1.7）按 R4.10 记录顺延 P8 release validation；
2. P1.6 缺口 3（broker 级专职 transition handler）归位 P2 `v1_control_adapter` 面（checkpoint 落档，避免同树双改 broker_ws/host_router）。

## 5. 签署

Dr. Lin 已于 2026-08-30 审阅本文档并确认无异议（“cp3-review 看过了，没问题”）——**P1 正式关闭**；P2 前置「Gate R + B + P1」全部满足，P2 无阻塞（mirror-vs-reuse 已决：reuse）。
